package c_sort.mergesort;

public class mergesort {
}

/*
int[] merged = new int[m + n];
        int i, j, k;
        i=j=k=0;

        while(i != m && j != n) {
            if(nums1[i] <= nums2[j]){
                merged[k++] = nums1[i++];
            } else {
                merged[k++] = nums2[j++];
            }
        }

        while(i != m) {
            merged[k++] = nums1[i++];
        }

        while(j != n) {
            merged[k++] = nums2[j++];
        }
*/
